import React from 'react';

const button = ({name}) => {
	return (
		<div className="row">
        
			<button id="defaultbutton" className="br-5 b-1s"> {name}</button>
  
		</div>
	);
};
export default button;